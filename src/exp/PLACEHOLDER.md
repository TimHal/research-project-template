# /exp

Place your experiment-specific code in this directory.
I consider it best practice to checkout individual branches for separate experiments to minimize
clutter. You might also consider writing experiment-specific `tasks` or `models`, but there are 
scenarios in which you will have to write code that does not really fit either semantic; therefore this
directory was created. 